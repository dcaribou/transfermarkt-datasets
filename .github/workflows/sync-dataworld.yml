name: sync-dataworld

on:
  workflow_run:
    workflows: ["build"]
    types: [completed]
    branches: [master]
  workflow_dispatch:

jobs:
  sync-dataworld:
    runs-on: ubuntu-latest
    container:
      image: dcaribou/transfermarkt-datasets:linux-amd64-dev # TODO: change to master
    defaults:
      run:
        shell: bash -l {0}
    # if: ${{ github.event.workflow_run.conclusion == 'success' }} TODO: uncomment
    steps:
      - run: |
          tree -a
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.PA_GITHUB_TOKEN }}
          fetch-depth: 2
      - run: |
          tree -a
          git config --global --add safe.directory /__w/transfermarkt-datasets/transfermarkt-datasets
      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v40
        with:
          files: |
            data/prep.dvc
      - name: pull data
        if: ${{ steps.changed-files.outputs.any_changed == 'true' }}
        run: |
          dvc pull
      - name: sync
        if: ${{ steps.changed-files.outputs.any_changed == 'true' }}
        run: |
          make \
            sync \
              SYNCER=dataworld
