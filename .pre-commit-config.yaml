repos:
  - repo: local
    hooks:
      - id: dbt-test
        name: dbt test
        language: system
        entry: bash -c 'cd dbt && ../.venv/bin/dbt test'
        pass_filenames: false
        files: ^dbt/
